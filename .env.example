# Firebase Configuration (Required for client-side authentication)
# These variables MUST have the VITE_ prefix to be accessible in the browser
# Get these values from your Firebase project console

# Firebase Web API Key (found in Project Settings > General > Web API Key)
VITE_FIREBASE_API_KEY=your_firebase_api_key_here

# Firebase App ID (found in Project Settings > General > Firebase SDK snippet > Config)
VITE_FIREBASE_APP_ID=your_firebase_app_id_here

# Server-side environment variables (no VITE_ prefix needed)
DATABASE_URL=postgresql://user:password@localhost:5432/hooklinestudio
OPENAI_API_KEY=your_openai_api_key_here
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# Firebase Admin SDK (for server-side operations)
FIREBASE_PROJECT_ID=hook-line-studio
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@hook-line-studio.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Railway Configuration (automatically set by Railway)
# RAILWAY_ENVIRONMENT=production
# RAILWAY_PROJECT_ID=your_railway_project_id
# RAILWAY_SERVICE_ID=your_railway_service_id

# Development
NODE_ENV=development
PORT=5000
LOG_LEVEL=info
ENABLE_REQUEST_LOGGING=true