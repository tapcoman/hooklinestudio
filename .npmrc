# Disable npm cache in production builds to prevent EBUSY errors
cache=/tmp/.npm
update-notifier=false
fund=false
audit=false
progress=false
# Use exact versions to ensure reproducible builds
save-exact=true
# Disable optional dependencies warnings
optional=false